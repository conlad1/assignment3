<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Searched Pokemon</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-black">
    <!-- full pokemon list -->
    <div class="container mt-5">
        <h3 class="mb-4 text-warning" id="pheader">Search Results</h3>
        <!-- error message -->
        <% if (typeof error_message !== 'undefined' && error_message) { %>           
            <h4 class="alert alert-danger" role="alert">
                <%= error_message %>
            </h4>
        <% } %>
        <!-- pokemon table => show error message if the table is empty -->
        <% if (pokemon.length === 0) { %> 
            <h4 class="alert alert-warning" role="alert"> 
                No pokemon found in the database 
            </h4>
        <% } else { %>
            <div class="table-responsive">
                <table class="table table-striped table-bordered">
                    <thead>
                        <tr class="table-dark">
                            <!-- this dynamically renders the headers -->
                            <% p_headers.forEach(h => { %>
                                <th>
                                    <%= h %>
                                </th>
                            <% }); %>
                        </tr>
                    </thead>
                    <tbody>
                        <!-- this dynamically reners the rows -->
                        <% pokemon.forEach(row => { %>
                            <tr>
                                <% p_headers.forEach(h => { %>
                                    <td>
                                        <%= row[h] == null ? '' : row[h] %>
                                    </td>
                                <% }); %>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        <% } %>
        <!-- Back to home button -->
        <div class="mb-4">
            <a href="/" class="btn btn-danger">Back to Pokemon Database</a>
        </div>
    </div>
</body>
</html>