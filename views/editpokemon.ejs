<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Pokemon</title>
    <!-- Bootstrap 5 CSS for styling -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-black">
    <div class="container mt-5">
        <!-- Form to edit an existing pokemon -->
        <h3 class="text-warning">Edit Pokemon</h3>
        <form action="/pokemon/edit/<%= pokemon.id %>" method="POST">
            <div class="mb-3">
                <label class="form-label text-info" for="description">Description</label>
                <input class="form-control" type="text" id="description" name="description" value="<%= pokemon.description %>" required>
            </div>
            <div class="mb-3">
                <label class="form-label text-info" for="base_total">Base Total</label>
                <input class="form-control" type="number" id="base_total" name="base_total" value="<%= pokemon.base_total %>" required>
            </div>
            <!-- Trainer field - handle case where trainer might be null -->
            <% if (pokemon.trainer !== null && pokemon.trainer !== undefined) { %>
            <div class="mb-3">
                <label class="form-label text-info" for="trainer">Trainer</label>
                <input class="form-control" type="text" id="trainer" name="trainer" value="<%= pokemon.trainer %>">
            </div>
            <% } else { %>
            <div class="mb-3">
                <label class="form-label text-info" for="trainer">Trainer</label>
                <input class="form-control" type="text" id="trainer" name="trainer" value="">
            </div>
            <% } %>
            <div class="mb-3">
                <button class="btn btn-danger" type="submit">Save</button>
            </div>
        </form>
        <br>
        <a href="/" class="btn btn-primary">Back to Pokemon List</a>
    </div>
</body>
</html>

