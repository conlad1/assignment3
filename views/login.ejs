<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <!-- Bootstrap 5 CSS for styling -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-black">
    <div class="container mt-5 border border-white rounded-3 p-3">
        <h3 class="text-center mb-4 text-warning">Login as Pokemon Trainer</h3>
        <br>
        <!-- Display error message if login failed -->
        <% if (error_message && error_message !== "") { %>
        <h3 class="text-center mb-4 text-danger" id="error_message" name="error_message"><%= error_message %></h3>
        <% } %>
        <br>  

        <!-- Login form - submits username and password to /login route -->
        <form class="mb-3" action="/login" method="POST">
            <div class="mb-3">
                <label class="form-label text-white" for="username">username</label>
                <input class="form-control" type="text" id="username" name="username" maxlength="30" autofocus required>
            </div>
            <div class="mb-3">
                <label class="form-label text-white" for="password">password</label>
                <input class="form-control" type="password" id="password" name="password" maxlength="30" required>
            </div>
            <div class="mb-3">
                <button class="btn btn-danger" type="submit">Login</button>
            </div>
        </form>

    </div>
</body>
</html>