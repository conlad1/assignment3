<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Trainer</title>
    <!-- Bootstrap 5 CSS for styling -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-black">
    <div class="container mt-5">
        <!-- Form to edit an existing trainer/user -->
        <h3 class="text-warning">Edit Trainer</h3>
        <form action="/users/edit/<%= user.username %>" method="POST">
            <div class="mb-3">
                <label class="form-label text-info" for="username">Username</label>
                <input class="form-control" type="text" id="username" name="username" value="<%= user.username %>" required>
            </div>
            <div class="mb-3">
                <label class="form-label text-info" for="password">Password</label>
                <input class="form-control" type="password" id="password" name="password" value="<%= user.password %>" required>
            </div>
            <div class="mb-3">
                <label class="form-label text-info" for="level">Level</label>
                <input class="form-control" type="text" id="level" name="level" value="<%= user.level %>" required>
            </div>
            <div class="mb-3">
                <button class="btn btn-danger" type="submit">Save</button>
            </div>
        </form>
        <br>
        <a href="/users" class="btn btn-primary">Back to Trainer List</a>
    </div>
</body>
</html>